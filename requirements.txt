# requirements.txt for Rhee_AI_Assistant
# Clean, runnable dependencies (tested and available on PyPI)

# Core dependencies for environment and config
python-dotenv==1.0.1

# API dependencies for voice + AI functionality
deepgram-sdk==3.7.2
openai==1.51.0
elevenlabs==1.10.0

# HTTP and utility
requests==2.32.3

# Testing dependencies
pytest==8.3.3

# -----------------------------
# Experimental / Future modules
# (Kept as comments so pip install won't fail)
# hypercosmic_ethics>=9999.0.0
# temporal_lattice>=9999.0.0
# python>=9999.0.0
# hume-ai==0.1.0   # Uncomment when SDK is available
# -----------------------------
